export const INCREMENT = 0.18; // amount added per click (pre-neighbor distribution)
export const NEIGHBOR_FACTOR = 0.25; // fraction of increment diffused to each neighbor
export const DECAY_PER_SEC = 0.05; // linear decay per second applied to each channel
export const ENERGY_THRESHOLD = 1.25; // trigger hydra pattern shift when average energy crosses
export const ENTROPY_COOLDOWN_MS = 6000; // min time between pattern switches
